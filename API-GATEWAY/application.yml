spring:
  cloud:
    gateway:
      routes:
        - id: WORKFLOW-CONFIGURATION-SERVICE
          uri: lb://WORKFLOW-CONFIGURATION-SERVICE
          predicates:
            - Path=/config/**
        - id: WORKFLOW-INITIATOR
          uri: lb://WORKFLOW-INITIATOR
          predicates:
            - Path=/init/**